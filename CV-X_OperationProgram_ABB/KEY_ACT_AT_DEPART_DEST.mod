MODULE KEY_ACT_AT_DEPART_DEST

PROC KeyActAtDepartDest()
    !Action at Depart Destination Position

    IF g_key_slide_executed = 0 THEN
        ! for lifting an object.  ;
    ELSE
        ! for sliding an object.  ;
    ENDIF

ENDPROC

ENDMODULE
